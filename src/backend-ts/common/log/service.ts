import { EventLogServiceModel, HardwareLogServiceModel } from "./model.ts";
import { eventLogRepository, hardwareLogRepository } from "./repository.ts";

export function eventLogService(log: EventLogServiceModel) {
  //   eventLogRepository();
}
export function hardwareLogService(log: HardwareLogServiceModel) {
  //   hardwareLogRepository();
}
